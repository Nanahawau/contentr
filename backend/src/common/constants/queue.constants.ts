export enum QueueName {
  UPLOAD = 'uploadQueue',
  TRANSCRIPTION = 'transcriptionQueue',
  GENERATE_CONTENT = 'generateContentQueue',
}

export enum JobName {
  TRANSCRIBE = 'transcribe',
  GENERATE_CONTENT = 'generateContent',
}